<h1>Blog entries</h1>

<table class="table">
  <tr>
    <th>Title</th>
    <th>Created on</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @blog_entries.each do |blog_entry| %>
  <tr>
    <td><%= blog_entry.title %></td>
    <td><%= blog_entry.time %></td>
    <td><%= link_to 'Show', blog_entry %></td>
    <td><%= link_to 'Edit', edit_blog_entry_path(blog_entry) %></td>
    <td><%= link_to 'Destroy', blog_entry, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />
<div class="action">
  <%= link_to 'New Blog entry', new_blog_entry_path , {:class => "btn btn-success"}%>
</div>